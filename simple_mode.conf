# llmcmd configuration for simple text generation
# Use this for direct text output without complex tool chains

openai_api_key = YOUR_API_KEY_HERE
model = gpt-4o-mini
temperature = 0.2
max_tokens = 2048

# Simple prompt for direct responses
system_prompt = You are a helpful assistant. Provide direct, concise answers without using complex tools unless absolutely necessary. Focus on giving the most straightforward response to the user's question.
