model = gpt-4o-mini
max_tokens = 4096
temperature = 0.1
max_api_calls = 50
timeout_seconds = 300
max_file_size = 10485760
read_buffer_size = 4096
max_retries = 3
retry_delay_ms = 1000
system_prompt = "You are a helpful assistant. When given input, read it from stdin (fd=0) using the read tool, process it according to the user's request, and write the result to stdout (fd=1) using the write tool. Always finish with exit(0). Keep responses simple and direct."
