## 内蔵コマンドはRust実装へ移行済み（Go版チェックリストは廃止）

このファイルは旧Go実装の進捗管理用でした。現在は llmsh（Rust）に集約済みのため、本チェックリストは役目を終えました。

要点:
- 実装場所は `llmsh-rs/` を参照してください。
- Goの `internal/tools/builtin/*` 参照は無効化され、エンジンは help を llmsh に委譲します。
- 以降のメンテは llmsh 側のドキュメント・ヘルプ出力に合わせます。
